{% extends 'base.html.twig' %}

{% block title %}Subject Heading{% endblock %}

{% block pageheader %}
    <h1>Subject Heading</h1>
{% endblock %}

{% block body %}
    <div class="btn-toolbar">
        <div class="btn-group pull-right">
            <a href="{{ path('subject_heading_index') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left"></span> Back
            </a>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('subject_heading_edit', { 'id': subjectHeading.id }) }}" class="btn btn-default">
                    <span class='glyphicon glyphicon-edit'></span> Edit
                </a>
                <a href="{{ path('subject_heading_delete', { 'id': subjectHeading.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this subject heading?">
                    <span class='glyphicon glyphicon-remove'></span> Delete
                </a>
            {% endif %}
        </div>
    </div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody><tr>
                <th>Subject Heading</th>
                <td>{{ subjectHeading.subjectHeading }}</td>
            </tr><tr>
                <th>Subject Heading URI</th>
                <td>{{ subjectHeading.subjectHeadingUri }}</td>
            </tr>
            <tr>
                <th>Books</th>
                <td>
                    {{ include('book/partial/list.html.twig', { books: subjectHeading.books }) }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
