{% extends 'base.html.twig' %}

{% block title %}Plate Type{% endblock %}

{% block pageheader %}
    <h1>Plate Type</h1>
{% endblock %}

{% block body %}
    <div class="btn-toolbar">
        <div class="btn-group pull-right">
            <a href="{{ path('plate_type_index') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left"></span> Back
            </a>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('plate_type_edit', { 'id': plateType.id }) }}" class="btn btn-default">
                    <span class='glyphicon glyphicon-edit'></span> Edit
                </a>
                <a href="{{ path('plate_type_delete', { 'id': plateType.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this plate type?">
                    <span class='glyphicon glyphicon-remove'></span> Delete
                </a>
            {% endif %}
        </div>
    </div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody><tr>
                <th>Plate Type</th>
                <td>{{ plateType.plateType }}</td>
            </tr><tr>
                <th>Plate Type Notes</th>
                <td>{{ plateType.plateTypeNotes }}</td>
            </tr>
            <tr>
                <th>Books</th>
                <td>
                    {{ include('book/partial/list.html.twig', { books: plateType.books }) }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
