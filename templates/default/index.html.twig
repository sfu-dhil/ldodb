{% extends 'base.html.twig' %}

{% block pageheader %}
    {% if homepage %}<h1>{{ homepage.title }}</h1>{% endif %}</h1>
{% endblock %}

{% block body %}

    {% include 'partial/search.html.twig' with {'route': 'search', 'label': 'Search'} %}

    {% if homepage %}
        {{ homepage.content|raw }}
    {% endif %}

{% endblock %}
