{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block pageheader %}
    <h1>Genre</h1>
{% endblock %}

{% block body %}
    <div class="btn-toolbar">
        <div class="btn-group pull-right">
            <a href="{{ path('genre_index') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left"></span> Back
            </a>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('genre_edit', { 'id': genre.id }) }}" class="btn btn-default">
                    <span class='glyphicon glyphicon-edit'></span> Edit
                </a>
                <a href="{{ path('genre_delete', { 'id': genre.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this genre?">
                    <span class='glyphicon glyphicon-remove'></span> Delete
                </a>
            {% endif %}
        </div>
    </div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody><tr>
                <th>Genre Name</th>
                <td>{{ genre.genreName }}</td>
            </tr><tr>
                <th>Genre Source</th>
                <td>{{ genre.genreSource }}</td>
            </tr><tr>
                <th>Genre Usage Note</th>
                <td>{{ genre.genreUsageNote }}</td>
            </tr><tr>
                <th>Genre URI</th>
                <td>{{ genre.genreUri }}</td>
            </tr><tr>
                <th>Broader Term ID</th>
                <td>{{ genre.broaderTermId }}</td>
            </tr>
            <tr>
                <th>Books</th>
                <td>
                    {{ include('book/partial/list.html.twig', { books: genre.books }) }}
                </td>
            </tr>
        </tbody>
    </table>
{% endblock %}
