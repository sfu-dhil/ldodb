{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>OtherNationalEdition</h1>
 {% endblock %} 

{% block body %}

    <div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('other_national_edition_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span> Back
        </a>
        <a href="{{ path('other_national_edition_edit', { 'id': otherNationalEdition.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit
        </a>
        <a href="{{ path('other_national_edition_delete', { 'id': otherNationalEdition.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this OtherNationalEdition?">
            <span class='glyphicon glyphicon-remove'></span> Delete
        </a>
    </div>
</div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Publicationdate</th>
            <td>{{ otherNationalEdition.publicationDate }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
