{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>DigitalCopyHolder</h1>
 {% endblock %} 

{% block body %}

    <div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('digital_copy_holder_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span> Back
        </a>
        <a href="{{ path('digital_copy_holder_edit', { 'id': digitalCopyHolder.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit
        </a>
        <a href="{{ path('digital_copy_holder_delete', { 'id': digitalCopyHolder.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this DigitalCopyHolder?">
            <span class='glyphicon glyphicon-remove'></span> Delete
        </a>
    </div>
</div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Organizationname</th>
            <td>{{ digitalCopyHolder.organizationName }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
