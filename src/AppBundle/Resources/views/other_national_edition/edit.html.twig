{% extends 'base.html.twig' %}

{% block title %}Other National Edition Edit{% endblock %}

{% block body %}
    <h1>Other National Edition Edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Update" class="btn btn-primary" />
            <a href="{{ path('other_national_edition_show', { 'id': otherNationalEdition.id }) }}" class="btn btn-warning">Cancel</a>
        </div>
    </div>
    {{ form_end(edit_form) }}

{% endblock %}
